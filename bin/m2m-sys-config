#!/usr/bin/env node

var SystemInitializer = require('../lib/system-initializer');

var initializer = new SystemInitializer({verbose: process.argv.length > 2 && _.startsWith(process.argv[2],'v')}).initNow();
