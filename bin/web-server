#!/usr/bin/env node

var httpServer = new require('../lib/http-server').start(process.env.PORT || '3000');
var socketServer = new require('../sockets/socket-server').start(httpServer);
var shellBehavior = new require('../sockets/shell-behavior').registerSelf(socketServer);
